{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile=false && pnpm --filter @wattson/memory exec prisma generate"
  },
  "deploy": {
    "startCommand": "pnpm --filter @wattson/memory exec prisma migrate deploy && pnpm --filter @wattson/api start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
